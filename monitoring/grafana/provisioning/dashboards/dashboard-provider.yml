apiVersion: 1

providers:
  - name: EduChallenge Dashboards
    orgId: 1
    folder: EduChallenge
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
