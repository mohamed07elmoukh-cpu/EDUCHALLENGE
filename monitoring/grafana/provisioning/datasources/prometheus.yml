apiVersion: 1

datasources:
  - name: EduChallenge Prometheus
    uid: educhallenge-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
